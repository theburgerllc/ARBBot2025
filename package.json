{
  "name": "arb-bot",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:arb": "hardhat run scripts/deploy.ts --network arbitrum",
    "deploy:opt": "hardhat run scripts/deploy.ts --network optimism"
  },
  "dependencies": {
    "ethers": "^6.7.0",
    "@flashbots/ethers-provider-bundle": "^1.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "hardhat": "^2.21.0",
    "@nomiclabs/hardhat-ethers": "^2.2.2",
    "@types/node": "^18.0.0",
    "typescript": "^4.9.5"
  }
}
